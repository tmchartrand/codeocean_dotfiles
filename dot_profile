# ~/.profile: executed by Bourne-compatible login shells.

if [ "$BASH" ]; then
  if [ -f ~/.bashrc ]; then
    . ~/.bashrc
  fi
fi

mesg n 2> /dev/null || true

export XDG_DATA_HOME=/root/capsule/code/.vscode
export GITHUB_TOKEN=$GIT_ACCESS_TOKEN